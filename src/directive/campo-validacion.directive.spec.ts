import { CampoValidacionDirective } from './campo-validacion.directive';

describe('CampoValidacionDirective', () => {
  it('should create an instance', () => {
    const directive = new CampoValidacionDirective();
    expect(directive).toBeTruthy();
  });
});
